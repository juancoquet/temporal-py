"""The ``example_job`` workflow: contract, definition, worker, and serve entrypoint.

A worked example — fans out over the plan the first activity returns, then reports how many items it
processed. Same four-module layout as an activity.
"""
